/**
 * 2020-01-18
 * As of current time, AVAILABLE is not really even used, in stead there are just null values. this probably needs to be fixed.
 */
export enum DaybookTimeScheduleStatus{
    SLEEP = 'SLEEP',
    TIMELOG_ENTRY = 'TIMELOG_ENTRY',
    AVAILABLE = 'AVAILABLE',  
}